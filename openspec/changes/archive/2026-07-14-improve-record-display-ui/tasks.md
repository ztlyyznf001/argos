## 1. 事件类型过滤

- [x] 1.1 在 `argos_packet_list_page.dart` 引入事件类型状态（全部/网络/崩溃/卡顿/资源），以 `record.kind` 为过滤依据，替换当前仅按 `method` 过滤的 `_filtered` 逻辑
- [x] 1.2 渲染事件类型过滤 chip 排，每个 chip 附带该类型在当前记录集中的条数
- [x] 1.3 将现有 Method chip 排降级为二级过滤：仅在事件类型为「全部」或「网络」时展示
- [x] 1.4 切换到非网络事件类型时把 Method 选择重置为 `ALL`，避免切回「网络」时残留过滤
- [x] 1.5 校验事件类型过滤与搜索、路由分组为 AND 关系，且过滤后的空分组不展示

## 2. 资源采样聚合

- [x] 2.1 实现聚合区间划分：在按时间排序的记录流中，把连续相邻的资源采样归为一组，遇到任何非资源事件即断开区间
- [x] 2.2 实现资源聚合项 Widget：折叠态展示区间时间范围、采样条数、末次 RSS 与**峰值** RSS，以及内存趋势（轻量自绘 sparkline 或数值表达，不引入图表库）
- [x] 2.3 实现展开态：列出区间内全部原始采样，每条可读出 RSS 与采样时刻
- [x] 2.4 长度为 1 的区间以普通单条记录形式展示，不呈现为需展开的聚合项
- [x] 2.5 校验聚合纯属展示层行为：存储中每条采样记录依然独立存在，未被删除或合并

## 3. 详情页按 kind 分派

- [x] 3.1 将 `ArgosPacketDetailPage` 改造为分派器：按 `record.kind` 选择详情视图，`network` 保持现有 `DefaultTabController` 双 Tab 结构不动（零回归）
- [x] 3.2 标题栏按 kind 分派：网络显示 Method 徽标 + URL；APM 事件显示类型徽标 + 事件摘要，不再出现空白 Method 徽标
- [x] 3.3 新增崩溃详情视图：异常消息、完整堆栈（可复制）、发生路由；无「请求/响应」Tab
- [x] 3.4 新增卡顿详情视图：丢帧数、区间总耗时、最大单帧耗时、`buildDuration` 与 `rasterDuration` 拆分；无「请求/响应」Tab
- [x] 3.5 新增资源详情视图：当前 RSS、峰值 RSS、采样时刻；CPU 不可得时留空而非占位值；无「请求/响应」Tab
- [x] 3.6 确认复制 cURL 按钮仍仅对 `kind == 'network'` 展示

## 4. 列表信息密度

- [x] 4.1 网络记录列表项补充响应体大小（`responseSize`），以人类可读单位展示（如 `1.2 KB`）
- [x] 4.2 时间展示改为「相对时间 · 绝对时刻」并列（如 `2 分钟前 · 14:03:22`），不引入常驻 Timer 刷新
- [x] 4.3 加强慢请求与错误状态码的视觉强调

## 5. 视觉与暗色模式

- [x] 5.1 将次要文字、分隔线、卡片背景的取色由硬编码灰阶（`Colors.grey` 等）改为从 `Theme.of(context).colorScheme` 取值
- [x] 5.2 保持 Method 与事件类型的语义色为固定色相，不从 `ColorScheme` 派生；仅按需调整深色模式下的明度
- [x] 5.3 统一卡片圆角、间距与字体层级
- [x] 5.4 空状态按当前选中的事件类型给出对应图标与文案，去除「抓包」假设

## 6. 验证

- [x] 6.1 运行 `flutter analyze` 确保无静态分析告警
- [x] 6.2 用 `example/lib/apm_demo_page.dart` 逐类触发事件（抛异常、制造卡顿、资源采样、发起 HTTP 请求），验证四类记录在列表中均可见且可按类型过滤
- [x] 6.3 验证资源采样在列表中折叠为聚合项，且中间夹杂 HTTP 请求或崩溃时聚合正确断开
- [x] 6.4 逐类点开详情页，验证崩溃/卡顿/资源均展示类型化内容且无「请求/响应」Tab，网络记录的双 Tab 与 cURL 复制无回归
- [x] 6.5 在浅色与深色主题下分别检查列表与详情页的对比度，确认无灰底灰字
