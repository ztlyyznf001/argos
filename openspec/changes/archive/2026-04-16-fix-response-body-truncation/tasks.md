## 1. 调整响应体存储策略

- [x] 1.1 在 `lib/model/kuril_http_info_model.dart` 中拆分请求体与响应体的 body 处理逻辑，移除文本响应体的 100KB 截断
- [x] 1.2 保持请求体超过 100KB 时仍追加 `"[TRUNCATED]"`，避免本次变更意外扩大请求体存储范围
- [x] 1.3 确认非文本响应仍不写入 `responseBody`，仅保留状态码、响应头和响应大小

## 2. 补充回归验证

- [x] 2.1 为大文本响应补充或更新测试，验证超过 100KB 的响应体写入后仍可完整读回
- [x] 2.2 为请求体截断行为补充回归测试，验证本次修改不会影响现有请求体保护逻辑
- [x] 2.3 验证详情页展示与复制响应体链路读取到的是完整 `responseBody`，不存在 `"[TRUNCATED]"` 误标记

## 3. 实施后检查

- [x] 3.1 运行与抓包模型、存储或 UI 相关的分析/测试命令，确认无新增错误
- [x] 3.2 使用一个超过 100KB 的文本响应做一次手工验证，确认新写入记录在详情页中显示完整内容
